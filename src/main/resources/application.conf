ktor {
    development = true
        deployment {
            port = 8080
            port = ${?PORT}
        }
        application {
            modules = [ santa.server.ApplicationKt.module ]
        }
}